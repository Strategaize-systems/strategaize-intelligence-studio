// Clay-CSV-Parser. Skeleton — Impl in SLC-105.

import type { ClayCsvParseResult } from "./types";

export function parseClayCsv(csvText: string): ClayCsvParseResult {
  void csvText;
  throw new Error(
    "clayCsvAdapter.parseClayCsv() not implemented (SLC-105 Lead Research)"
  );
}

export const clayCsvAdapter = { parseClayCsv };
