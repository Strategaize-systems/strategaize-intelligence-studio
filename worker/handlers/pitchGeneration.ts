// pitch_generation handler — Skeleton.
// Impl in SLC-106 (Messaging-Variation). 4-Level-Personalization,
// optional psychology_boosters, schreibt pitch_version + ai_cost_ledger.

import type { AiJob } from "../types";

export async function handlePitchGeneration(job: AiJob): Promise<void> {
  void job;
  throw new Error(
    "handlePitchGeneration not implemented (SLC-106 Messaging-Variation)"
  );
}
